[package]
name = "tanaid-wasm"
version = "0.1.0" 
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
tanaid = { path = "../..", default-features = false }
wasm-bindgen = "0.2"
